package com.globe.restservices.repository;

import com.globe.restservices.model.Where;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WhereRepository extends JpaRepository<Where, Integer> {
}
