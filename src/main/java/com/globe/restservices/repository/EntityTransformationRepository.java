package com.globe.restservices.repository;

import com.globe.restservices.model.EntityTransformation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EntityTransformationRepository extends JpaRepository<EntityTransformation, Integer> {
}
